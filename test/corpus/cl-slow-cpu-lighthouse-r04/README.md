# cl-slow-cpu-lighthouse-r04

The Lighthouse consensus client's CPU is capped to 9% of one core for the full duty window while geth remains unthrottled. Exact per-slot Engine counters separate consensus validation time from execution latency, and Lighthouse-side proposers are excluded so proposal production cannot confound the watched attestation. RE-TUNED 2026-08-26 for the three-node devnet. At quota_percent 9 this recipe produced a healthy duty on both campaign rounds (r02, r03) — verdict "" at high confidence, block_found and included both true. The recipe was not broken; the devnet underneath it changed. With a third participant the throttled node receives each block from two peers instead of one, so it still observes the block in time even with its CPU cut, and beacon_block_delay_observed_slot_start never slips past the deadline. The sibling cl-slow-cpu recipe passed 2/2 in the same campaign at quota_percent 7, which brackets the answer: 9 is now too loose and 7 still bites.
Bisection log (quota_percent / result):
   9 / healthy, no cause — 0/2 on the three-node devnet (was tuned on two)
   5 (current) / pending — below the 7 that still works, for margin against a
                           devnet that now also carries transaction load


## What was broken

Fault: cgroup_cpu applied to cl-1-lighthouse-geth for 25s, around slot 1866.

## Recorded outcome

- Block observed for the duty slot: true
- Attestation published (seen in the pool): true
- Attestation included: true
- Source recipe: cl-slow-cpu-lighthouse

## Expected taxonomy label

- cause: local.cl_slow
- sub_cause: 
- confidence: medium

Generated by tools/faultinjector against a live Kurtosis devnet
(test/e2e/kurtosis). See manifest.yaml for full provenance and
observations.jsonl for the raw recorded facts.
