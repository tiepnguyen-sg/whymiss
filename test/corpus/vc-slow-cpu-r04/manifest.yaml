corpus_format_version: 2
generator_engine_version: 0.13.0
id: vc-slow-cpu-r04
recipe_id: vc-slow-cpu
description: |
    LABEL CORRECTED 2026-08-26. The recipe aimed at its own cause, but this run
    produced the ambiguous shape instead: a proven canonical skip together with no
    attestation_published and no attestation_included at all. ADR-0015 notes an
    attester publishes and is included normally on a skipped slot, so the skip does
    not explain the missing attestation, and nothing here separates "only the
    upstream proposer failed" from "it failed and the local path failed too". R-400
    cannot settle it either — it needs block_seen and head_updated before the
    deadline to establish the beacon node was healthy, and a skipped slot has
    neither. Per ADR-0021 the honest label is unknown.insufficient_data at low
    confidence. Derived from the observations against the taxonomy, not from what the
    engine happens to output. Original recipe intent is preserved below.

    The Lighthouse validator client's CPU is capped to 0.1% of one core (cgroup v2 cpu.max) for the full duty window — unlike vc-frozen-lighthouse's pause, the process keeps running and should eventually publish, just late, isolating local.vc_slow (published late, head received on time) from local.vc_disconnected (never published at all). Integer 1% (the previous value) left the duty healthy twice in a row on this devnet: BLS signing needs only a few ms of actual CPU time, so even throttled to 1ms per 100ms period it only adds a few hundred ms of wall-clock delay — nowhere near the ~4s attestation deadline. 0.1% (100us/100ms period) stretches that same handful of ms of real work into multiple seconds of wall-clock delay.
expect:
    cause: unknown.insufficient_data
    confidence: low
slot: 11395
validator_index: 2
fault_kind: cgroup_cpu
fault_target: vc-1-geth-lighthouse
duration: 35s
generated_at: 2026-08-25T12:21:11.949380249Z
clock_samples:
    - server: pool.ntp.org:123
      sampled_at: 2026-08-25T12:08:15.10274682Z
      offset: -675.105µs
      round_trip: 3.562675ms
    - server: pool.ntp.org:123
      sampled_at: 2026-08-25T12:21:11.94890402Z
      offset: -334.052µs
      round_trip: 4.524839ms
observations_sha256: 5168ad5a850850f099b1d204c1f1810f8607566ba27f64de7b583dae9f0924d0
