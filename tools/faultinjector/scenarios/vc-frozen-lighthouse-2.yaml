id: vc-frozen-lighthouse-2
description: >
  A second, independent run of the same fault as vc-frozen-lighthouse — the
  Lighthouse validator client's container frozen for the full duty window —
  against a different validator and slot, for redundancy in the corpus.
target: vc-1-geth-lighthouse
validator_candidates: [0, 31]
duration: 20s
avoid_proposer_validators: [0, 31]
fault:
  kind: pause
  pause: {}
expect:
  cause: local.vc_disconnected
  confidence: high
